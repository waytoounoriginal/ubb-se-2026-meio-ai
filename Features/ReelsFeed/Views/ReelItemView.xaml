<UserControl
    x:Class="ubb_se_2026_meio_ai.Features.ReelsFeed.Views.ReelItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:ubb_se_2026_meio_ai.Core.Models">

    <Grid Background="Black">
        <!-- The Media Player -->
        <MediaPlayerElement x:Name="ReelPlayer"
                            AreTransportControlsEnabled="False"
                            Stretch="UniformToFill" />

        <!-- Right-side action bar (TikTok/Instagram style) -->
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center"
                    Margin="0,0,16,0" Spacing="4">
            <Button x:Name="LikeButton"
                    Click="LikeButton_Click"
                    Width="48" Height="48"
                    CornerRadius="24"
                    Padding="0"
                    Background="Transparent"
                    BorderBrush="Transparent">
                <FontIcon x:Name="HeartIcon"
                          FontFamily="Segoe MDL2 Assets"
                          Glyph="&#xEB51;"
                          FontSize="26"
                          Foreground="White" />
            </Button>
            <TextBlock x:Name="LikeCountText"
                       Text="0"
                       Foreground="White"
                       FontSize="13" FontWeight="SemiBold"
                       HorizontalAlignment="Center" />
        </StackPanel>

        <!-- Bottom Overlay -->
        <Grid VerticalAlignment="Bottom" Height="250">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="Transparent" Offset="0" />
                    <GradientStop Color="#EE000000" Offset="1" />
                </LinearGradientBrush>
            </Grid.Background>
            <StackPanel VerticalAlignment="Bottom" Margin="16,0,16,32">
                <TextBlock Text="{x:Bind Reel.Title, Mode=OneWay}"
                           Foreground="White" FontSize="22" FontWeight="Bold" />
                <Border Background="#33FFFFFF" CornerRadius="4" HorizontalAlignment="Left" Margin="0,8,0,0" Padding="8,4">
                    <TextBlock Text="{x:Bind Reel.Caption, Mode=OneWay}" Foreground="White" FontSize="14" />
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
